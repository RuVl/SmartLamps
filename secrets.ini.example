; Copy to secrets.ini (gitignored). Values here override the [secrets] section
; in platformio.ini.
[secrets]
; Password of the lamp's own access point (SSID = lamp name, e.g. lamp-a4f2).
; WPA2 needs at least 8 characters.
ap_pass = smartlamp
